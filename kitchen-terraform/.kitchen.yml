---
driver:
  name: terraform
  parallelism: 4
     
provisioner:
  name: terraform

verifier:
  name: terraform

platforms:
  - name: terraform

suites:
  - name: vpc_suite
    driver:
      root_module_directory: test/fixtures/tf_module
      variables:
        project_id: searce-playground
    verifier:
      systems:
        - name: vpc
          backend: local
